#include <stdio.h>
int main () {
    int i,j,k,h;
    for(i=1;i<=5;i++)
    j=1+2+3+4+5;
    {
        for(i=1;i<=5;i++)
        {
            printf("square of the number is: %d\n",i*i);
        }
        printf("sum of first five numbers is: %d\n",j);
        for(i=1;i<=5;i++)
        h=1+4+9+16+25;
        {
            printf("sum of squares is: %d",h);
        }
    }
    return 0;
}